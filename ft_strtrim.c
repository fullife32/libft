#include "libft.h"

char        *ft_strtrim(char const *s1, char const *set)
{
    int     i;
    char    *str;

    i = 0;
    if (!(str = malloc(sizeof(char) * (ft_strlen(s1) + 1))))
        return (0);
    while (s1[i])
    {
        
    }
}